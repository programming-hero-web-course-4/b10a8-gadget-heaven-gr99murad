import React from 'react';
import Navbar from '../Navbar/Navbar';

const MainLayouts = () => {
    return (
        <div>
            <Navbar></Navbar>
        </div>
    );
};

export default MainLayouts;